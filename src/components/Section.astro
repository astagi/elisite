---
const { background, isFullHeight } = Astro.props;
---

<div class:list={[background, { 'h-screen': isFullHeight }]}>
    <div class="mx-auto w-full max-w-screen-xl" class:list={{ 'p-8': !isFullHeight }}>
        <slot></slot>
    </div>
</div>